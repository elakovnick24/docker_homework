version: '3.7'
services:
  mysql:
    image: mysql:8.0.18

    ports:
       - '3306:3306'
    volumes:
       - ./data:/var/lib/mysql
    environment:
       - POSTGRES_DB=postgres
       - PGPASSWORD=password
       - POSTGRES_USER=
